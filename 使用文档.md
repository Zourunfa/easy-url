# YF-URL 插件使用文档

## 📖 插件简介

YF-URL 是一个强大的浏览器扩展工具，专为简化 URL 参数管理和 Portal 页面操作而设计。支持 URL 参数可视化编辑、Portal 页面 Tab 信息提取等功能。

---

## ✨ 主要功能

### 1.

Portal 模式专门用于解析和管理企业门户页面的 Tab 信息。

#### 功能特性：
- **自动解析 Tab 列表**：自动识别页面中的所有 Tab 标签
- **提取 iframe 地址**：获取每个 Tab 对应的 iframe URL
- **参数提取**：自动提取并展示关键参数
  - `_openx_header` 参数
  - `token` 参数
- **快捷操作**：
  - 📋 **复制**：一键复制 iframe 地址或参数值
  - 🔗 **跳转**：在新标签页中打开 iframe 地址
  - 🔄 **刷新**：重新加载当前页面的 Tab 信息

#### 使用场景：
- 快速查看门户页面中所有子系统的访问地址
- 提取和复制系统间传递的认证参数
- 调试和测试各个子系统的独立访问

#### 使用方法：
1. 打开包含 "portal" 关键词的门户页面
2. 点击浏览器工具栏的插件图标，打开侧边栏
3. 默认进入 Portal 模式，自动加载 Tab 信息
4. 查看每个 Tab 的详细信息：
   - 左侧：iframe 完整地址
   - 右侧：提取的 `_openx_header` 和 `token` 参数
5. 使用"复制"按钮复制需要的信息
6. 使用"跳转"按钮在新标签页打开子系统

---

### 2. Local 模式

Local 模式用于当前页面的 URL 参数编辑和管理。

#### 功能特性：
- **URL 可视化编辑**：直接编辑完整的 URL 地址
- **_openx_header 参数管理**：
  - 查看原始 Base62 编码的参数值
  - JSON 可视化编辑器，支持结构化编辑
  - 自动编码/解码转换
- **实时预览**：修改后实时更新 URL
- **快捷操作**：
  - 🔄 **刷新当前 URL**：用编辑后的 URL 刷新当前页面
  - 🆕 **跳转新 TAB**：在新标签页打开编辑后的 URL
  - 🔃 **刷新数据**：重新获取当前页面的 URL 和参数

#### 使用场景：
- 调试和修改 URL 参数
- 编辑复杂的 `_openx_header` 参数
- 测试不同参数组合的页面效果

#### 使用方法：
1. 在任意页面打开插件侧边栏
2. 切换到 "Local 模式" 标签
3. 在文本框中编辑 URL 或 `_openx_header` 参数
4. 使用 JSON 编辑器修改参数结构
5. 点击"刷新当前 URL"应用修改，或"跳转新 TAB"在新页面测试

---

## 🚀 安装步骤

### 前置要求
- Node.js (推荐 v16 或更高版本)
- pnpm 包管理器
- Chrome 或 Edge 浏览器

### 步骤 1：安装依赖

```bash
# 进入项目目录
cd yf-url

# 安装依赖
pnpm install
```

### 步骤 2：构建插件

```bash
# 开发模式（支持热更新）
pnpm run dev

# 或生产模式
pnpm run build
```

构建完成后，会在项目根目录生成 `extension` 文件夹。

### 步骤 3：加载到 Chrome 浏览器

1. **打开扩展程序管理页面**
   - 在 Chrome 地址栏输入：`chrome://extensions/`
   - 或点击：菜单 → 更多工具 → 扩展程序

2. **启用开发者模式**
   - 在页面右上角找到"开发者模式"开关
   - 点击开关，启用开发者模式

3. **加载未打包的扩展程序**
   - 点击左上角"加载已解压的扩展程序"按钮
   - 选择项目中的 `extension` 文件夹
   - 点击"选择文件夹"

4. **确认安装成功**
   - 扩展程序列表中出现 "yf-url" 插件
   - 浏览器工具栏显示插件图标

### 步骤 4：固定插件图标（可选）

- 点击浏览器工具栏右侧的拼图图标
- 找到 "yf-url" 插件
- 点击图钉图标，将插件固定到工具栏

---

## 💡 使用技巧

### 1. 快速访问
- 点击工具栏图标即可打开侧边栏
- 侧边栏会记住上次使用的模式

### 2. Portal 模式最佳实践
- 确保页面 URL 包含 "portal" 关键词
- 等待页面完全加载后再点击刷新
- 如果 Tab 信息未正确显示，尝试刷新页面后重新加载

### 3. Local 模式最佳实践
- 修改参数前先复制原始 URL 备份
- 使用 JSON 编辑器可以更直观地修改复杂参数
- 建议先在新 TAB 测试，确认无误后再刷新当前页面

### 4. 参数复制技巧
- 点击"复制"按钮后会显示成功提示
- 复制的内容可直接粘贴到其他工具或文档中
- `_openx_header` 参数已自动解码，复制的是 JSON 格式

---

## 🔧 开发模式

如果需要修改插件代码：

```bash
# 启动开发模式
pnpm run dev

# 修改代码后，浏览器会自动重新加载插件
# 如果未自动更新，可以在 chrome://extensions/ 页面点击刷新按钮
```

---

## 📝 注意事项

1. **权限要求**
   - 插件需要访问所有网站的权限以读取页面内容
   - 首次使用时浏览器可能会提示授权

2. **兼容性**
   - 支持 Chrome、Edge 等基于 Chromium 的浏览器
   - Portal 模式仅在包含 "portal" 关键词的页面生效

3. **数据安全**
   - 所有数据处理均在本地完成
   - 不会上传任何信息到服务器

4. **更新插件**
   - 拉取最新代码后重新执行 `pnpm run build`
   - 在 `chrome://extensions/` 页面点击插件的刷新按钮

---

## 🐛 常见问题

### Q: 插件图标显示但点击无反应？
A: 检查是否在支持的页面上使用，或尝试刷新页面后重试。

### Q: Portal 模式显示"未找到 tab 列表元素"？
A: 确保页面 URL 包含 "portal" 且页面已完全加载，可以尝试点击刷新按钮。

### Q: 参数提取显示"(无)"？
A: 说明该 iframe URL 中不包含对应的参数，这是正常现象。

### Q: 修改后的 URL 不生效？
A: 检查 URL 格式是否正确，确保参数编码无误。

---

## 📞 技术支持

如有问题或建议，请联系开发团队或提交 Issue。

---

## 📄 更新日志

### v0.0.1
- ✅ 实现 Portal 模式 Tab 信息提取
- ✅ 实现 Local 模式 URL 参数编辑
- ✅ 支持 `_openx_header` 和 `token` 参数提取
- ✅ 添加复制、跳转等快捷操作
- ✅ 优化用户界面和交互体验
